/**
 * Router — Barrel Export
 */
export { useHashRouter } from './useHashRouter';
export { matchRoute, buildPath } from './routes';
export type { RouteMatch } from './routes';
export type { HashRouter } from './useHashRouter';
